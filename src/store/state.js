import { PLAY_MODE } from '@/assets/js/constant'

const state = {
    squenceList: [],
    playList: [],
    playing: false,
    playMode: PLAY_MODE.squence,
    currentIndex: 0,
    fullScreen: false
}

export default state
